# gtsam_consumer

I added this line to `build.gradle`:

```
implementation "edu.wpi.first.thirdparty.frc2025.gtsam:gtsam-cpp:4.3-3"
```


See **hello_remote_consumer** for notes.




Uses the vendordep made in hello-vendordep.

It does not have `useLocal` in build.gradle, so it uses the maven
files from `github.io`.


